{
  "event": "{{ event }}",
  "timestamp": "{{ timestamp }}",
  "transaction": {
    "id": "{{ transaction.id }}",
    "status": "completed",
    "amount_cngn": "{{ transaction.amount_cngn }}",
    "wallet_address": "{{ transaction.wallet_address }}",
    "stellar_tx_hash": "{{ transaction.tx_hash }}",
    "explorer": "{{ stellar_explorer(transaction.tx_hash) }}"
  }
}

